<Plugin "python">
    ModulePath "/usr/lib64/collectd/"

    Import "ceph_cluster_iops"

    <Module "ceph_cluster_iops">
        Verbose "True"
        cluster_id "{{ cluster_id }}"
        cluster_name "{{cluster_name}}"
        Interval 30
    </Module>
</Plugin>
